====
BUGS
====

* la commande closeallpos ne marche pas correctement, le moneymanager ne renvoie pas la bonne liste de positions avec findPos()
* closepos, la commande fonctionne de façon aléatoire, la commande est envoyée en boucle alors que la posiion semble deja fermée.
* La synchronization des positions ne fonctionne pas bien: fonctionne au demarrage mais apres ça se complique.

===========
PRIORITAIRE
===========

* LOGS BROKER !
* Implementer le client LightStreamer dans igconnector !
* faire une fonction records_sample "loose" au cas ou le timestamp ne serait pas trouvé ( lorsque tick > 1s par ex.)
* Inclure une option safe_signal dans les backtests qui verifie le delta en secondes entre v[t] et v[t-1], et qui jump si le delta est trop grand ( > 5s)
* Finir le format de backtests JSON
* faire une fonction ou une variable de eval_io qui renvoie le close de la veille.
* ajouter un argument "guaranteed_stop" aux buy et sell orders ?

==========
SECONDAIRE
==========

* Finir le module deepthroat pour le news analysis
* Completer la doc

